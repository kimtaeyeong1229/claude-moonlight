# Moonlight v0.4 — 다음 작업

> v0.3 이후 작업. 진행 상태: **1·2·3 모두 반영** (아래 ✅ 표시).

## 1. OS별 실행 방식 분기 (설치 시)  ✅ 구현
- `scripts/install.sh` 가 `uname -s` 로 분기: macOS→네이티브 앱(`make-app.sh`),
  Linux→웹 모드(`moonlight-web.sh` + `.desktop`), Windows→`moonlight-web.bat`(웹).
- macOS 에서 동작 확인. Linux/Windows 런처는 작성 완료(해당 OS 실기 테스트는 미수행).
- Windows 한계: 번역 워커(PTY/fork)는 POSIX 전용 → Windows 호스트 불가, **WSL2 권장**(문서화).
- 설치/실행 시 **운영체제를 파악**해서:
  - **macOS** → 지금처럼 **네이티브 앱**(`/Applications/claude-moonlight.app`, WKWebView).
  - **Windows / Linux** → 앱 대신 **웹으로 실행**(컨테이너 기동 후 브라우저로 `http://127.0.0.1:8090` 열기).
- 이유: PTY/fork/launchd/swiftc 기반 네이티브 앱은 macOS 전용. 다른 OS는 컨테이너 + 브라우저가 현실적.
- 작업 메모:
  - `make-app.sh`(또는 통합 `install` 스크립트)에서 `uname`/OS 감지 → 분기.
  - Windows/Linux 용 런처: 컨테이너 `up` + 기본 브라우저 open(`xdg-open`/`start`).
  - 단, 번역 워커는 여전히 **호스트 claude 인증** 필요 → OS별 claude 위치/실행 방식 점검.

## 2. 레포 public 전환  ✅ 완료
- `github.com/kimtaeyeong1229/claude-moonlight` → **PUBLIC** 전환 완료.
- 전환 전 시크릿 스캔(키/토큰 없음)·`data/` git 제외 재확인 완료.
- (남은 선택사항) LICENSE 추가 — 미정.

## 3. 컨테이너 재시작에도 DB(논문) 영속 — 레포 위치에 차곡차곡 적재  ✅ 검증
- `./data:/data` 바인드 마운트로 `papers/`·`jobs/`·`papers.json` 이 레포 내 `claude-moonlight/data/` 에 영속.
- **회귀 테스트 통과**: 컨테이너 `down` → `up` 후에도 논문 목록 그대로 유지 확인.
- 즉 "레포 위치에 차곡차곡 적재"는 **현재 파일 기반 설계로 이미 충족**. (`data/` 는 제3자 논문 발췌 포함이라 git 제외.)
- 후속 선택(미착수): 규모 커지면 SQLite 인덱스(`data/moonlight.db`) 전환, 논문 export/백업 기능.

---
### 비고
- 위 3건은 **v0.4 착수 시** 우선순위 재확인 후 진행. 현재는 기록만.
